# Identity
nameOverride: ""
namespace: "vaultrix"
appName: "review-service"
component: "backend"
partOf: "vaultrix"
environment: "dev"

replicaCount: 1

image:
  repository: "REGISTRY/vaultrix-review-service" # TODO: set your image registry
  tag: "latest"
  pullPolicy: IfNotPresent

imagePullSecrets: []

serviceAccount:
  create: true
  name: "review-service"
  annotations: {}

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
securityContext: {}

service:
  name: "review-service"
  type: ClusterIP
  port: 3006
  targetPort: 3006
  appProtocol: http

container:
  name: "review-service"
  port: 3006

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

probes:
  liveness:
    enabled: true
    path: /health
    initialDelaySeconds: 15
    periodSeconds: 10
  readiness:
    enabled: true
    path: /health
    initialDelaySeconds: 10
    periodSeconds: 10

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80

config:
  enabled: true
  mapName: "review-service"
  map:
    PORT: "3006"
    ORDER_SERVICE_URL: "http://order-service:3002"


secret:
  enabled: true
  create: true
  name: "review-service"
  stringData:
    MONGO_URI: "# TODO: set MONGO_URI"


httpRoute:
  enabled: false
  name: "review-service-route"
  parentRefs: []
  hostnames: []
  rules: []
