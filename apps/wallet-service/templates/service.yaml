apiVersion: v1
kind: Service
metadata:
  name: {{ include "wallet-service.name" . }}
  labels:
    app: {{ include "wallet-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "wallet-service.name" . }}
