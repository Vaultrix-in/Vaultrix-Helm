environment: "dev"
namespace: "vaultrix-dev"

secret:
  stringData:
    MONGO_URI: "mongodb://root:<password>@mongodb.vaultrix-dev.svc.cluster.local:27017/wallet_db?authSource=admin" # TODO: replace <password>
