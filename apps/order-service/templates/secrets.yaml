apiVersion: v1
kind: Secret
metadata:
  name: {{ include "order-service.fullname" . }}-secret
type: Opaque
stringData:
  {{- range $key, $val := .Values.secret.stringData }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
