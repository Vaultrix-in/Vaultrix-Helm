apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ include "order-service.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
  annotations:
    sealedsecrets.bitnami.com/managed: "true"
spec:
  encryptedData:
    # Encrypted placeholders - Run the following commands to update these:
    # echo -n "VALUE" | kubeseal --raw --name {{ include "order-service.fullname" . }}-secret --namespace {{ .Release.Namespace }}
    MONGO_URI: AgBy3iG...[SEALED_DATA_FOR_MONGO_URI]...
  template:
    type: Opaque
    metadata:
      name: {{ include "order-service.fullname" . }}-secret
      namespace: {{ .Release.Namespace }}
