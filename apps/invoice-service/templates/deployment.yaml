apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "invoice-service.fullname" . }}
  labels:
    app: {{ include "invoice-service.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "invoice-service.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "invoice-service.name" . }}
    spec:
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "invoice-service.fullname" . }}-config
            - secretRef:
                name: {{ include "invoice-service.fullname" . }}-secret
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
