apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: frontend-route
  namespace: vaultrix-dev
spec:
  parentRefs:
    - name: shared-gateway
  hostnames:
    - vaultrix.dev.internal # TODO: set actual hostname
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: frontend
          port: 80
